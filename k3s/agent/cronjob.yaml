apiVersion: batch/v1
kind: CronJob
metadata:
  name: match-scraper-agent
  namespace: match-scraper
spec:
  schedule: "0 2,8,14,20 * * *"  # 4x/day every day — see cronjob-weekend.yaml for the hourly Sat 15:00 → Sun 20:00 ET cadence layered on top
  # Stated, not inherited (SB-1060). A schedule with no timeZone is read in
  # the kube-controller-manager's local zone, which on rancher-desktop is
  # America/New_York — so these have always been ET hours, and the comment
  # that called them UTC was wrong for as long as it stood. Naming the zone
  # changes no fire time; it stops the next reader doing the arithmetic in
  # the wrong zone, which is exactly how SB-1060 happened.
  timeZone: America/New_York
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 1
      activeDeadlineSeconds: 1800
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: agent
              image: ghcr.io/silverbeer/match-scraper:latest
              imagePullPolicy: Always
              args: ["run", "--env", "prod"]
              env:
                # Feed bodies live beside the run journal on the PVC, so an
                # unchanged schedule feed costs a 304 instead of 7 MB from
                # someone else's CloudFront distribution (SB-884). Unset, or
                # pointed at a path with no volume behind it, the cache simply
                # does nothing.
                - name: ASSIST_FEED_CACHE_DIR
                  value: /data/agent-state/assist-feeds
              envFrom:
                - configMapRef:
                    name: match-scraper-agent-config
                - secretRef:
                    name: match-scraper-agent-secret
              volumeMounts:
                # The run journal lives here — the previous run's per-target
                # results, which the modifier rules read to retry a target
                # that errored. PR #57 removed this mount when the journal
                # moved to S3; the cluster kept it, and SB-555 made the code
                # use it again. Without it the agent silently loses its
                # cross-run memory.
                - name: agent-state
                  mountPath: /data/agent-state
              resources:
                requests:
                  cpu: 200m
                  memory: 1Gi
                limits:
                  cpu: "1"
                  memory: 2Gi
          volumes:
            - name: agent-state
              persistentVolumeClaim:
                claimName: agent-state
