apiVersion: v1
kind: ConfigMap
metadata:
  name: missing-table-worker-prod-config
  namespace: match-scraper
  labels:
    app: missing-table-worker
    environment: prod
data:
  # Celery Configuration
  CELERY_BROKER_URL: "amqp://admin:admin123@messaging-rabbitmq.match-scraper:5672//"
  CELERY_RESULT_BACKEND: "rpc://"

  # RabbitMQ Configuration
  RABBITMQ_URL: "amqp://admin:admin123@messaging-rabbitmq.match-scraper:5672//"

  # Queue Configuration - prod queue (writes to prod Supabase serving missingtable.com)
  QUEUE_NAME: "matches.prod"

  # Supabase Configuration (prod — serves missingtable.com)
  SUPABASE_URL: "https://ppgxasqgqbnauvxozmjw.supabase.co"

  # Application Configuration
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
