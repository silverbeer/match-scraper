# ⚠️ DEPRECATED - This configmap is no longer used
# As of 2025-11-14, missingtable.com points to dev environment only
# This file is kept for reference only

apiVersion: v1
kind: ConfigMap
metadata:
  name: missing-table-worker-prod-config
  namespace: match-scraper
  labels:
    app: missing-table-worker
    environment: prod
    deprecated: "true"
data:
  # Celery Configuration
  CELERY_BROKER_URL: "amqp://admin:admin123@messaging-rabbitmq.match-scraper:5672//"
  CELERY_RESULT_BACKEND: "rpc://"

  # RabbitMQ Configuration
  RABBITMQ_URL: "amqp://admin:admin123@messaging-rabbitmq.match-scraper:5672//"

  # Queue Configuration - PROD queue only
  QUEUE_NAME: "matches.prod"

  # Supabase Configuration (PROD)
  SUPABASE_URL: "https://iueycteoamjbygwhnovz.supabase.co"

  # Application Configuration
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
